import { FC } from "react";

import { AppRoutes } from "../../routes/index";

const App: FC = () => {
  return (
    <div className="App">
      <AppRoutes />
    </div>
  );
};

export default App;
